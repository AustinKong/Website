import LetterGrid from './LetterGrid'
import style from './index.module.css'

const Hero = () => {
  return (
    <section
      className={style.hero}
    >
      <LetterGrid />
    </section>
  )
}

export default Hero